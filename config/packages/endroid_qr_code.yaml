endroid_qr_code:
    default:
        # SVG avoids requiring the PHP GD extension (PngWriter needs GD).
        writer: Endroid\QrCode\Writer\SvgWriter
        size: 300
        margin: 10
        encoding: 'UTF-8'
        error_correction_level: 'low'
        round_block_size_mode: 'margin'
        validate_result: false
