{% extends 'back/base.html.twig' %}

{% block title %}Vente #{{ vente.id }} - Admin AgriGo{% endblock %}

{% block content %}
<div class="space-y-6">
    {# Header #}
    <div class="flex flex-col md:flex-row justify-between items-start md:items-center gap-4">
        <div>
            <p class="text-xs font-semibold tracking-widest uppercase text-agri-green mb-1">Administration</p>
            <h1 class="text-3xl font-serif font-bold text-gray-900">Détails de la vente #{{ vente.id }}</h1>
            <p class="text-gray-500 mt-1 text-sm">Informations complètes sur cette vente.</p>
        </div>
        <div class="flex gap-3">
            <a href="{{ path('admin_vente_edit', {'id': vente.id}) }}" class="inline-flex items-center gap-2 px-4 py-2 bg-blue-600 text-white text-sm font-medium rounded-xl hover:bg-blue-700 transition shadow-sm">
                <i class="ph ph-pencil"></i> Modifier
            </a>
            <a href="{{ path('admin_vente_index') }}" class="inline-flex items-center gap-2 px-4 py-2 bg-gray-100 text-gray-700 text-sm font-medium rounded-xl hover:bg-gray-200 transition">
                <i class="ph ph-arrow-left"></i> Retour
            </a>
        </div>
    </div>

    {# Details Card #}
    <div class="bg-white border border-gray-100 rounded-2xl shadow-sm overflow-hidden">
        <div class="p-8">
            <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
                {# Left Column #}
                <div class="space-y-6">
                    <div>
                        <h3 class="text-sm font-semibold text-gray-400 uppercase tracking-wider mb-2">Informations générales</h3>
                        <div class="space-y-4">
                            <div>
                                <p class="text-sm text-gray-500">ID de la vente</p>
                                <p class="text-lg font-semibold text-gray-900">#{{ vente.id }}</p>
                            </div>
                            <div>
                                <p class="text-sm text-gray-500">Description</p>
                                <p class="text-lg text-gray-900">{{ vente.description }}</p>
                            </div>
                            <div>
                                <p class="text-sm text-gray-500">Prix</p>
                                <p class="text-2xl font-bold text-emerald-600">{{ vente.price|number_format(2, ',', ' ') }} €</p>
                            </div>
                        </div>
                    </div>
                </div>

                {# Right Column #}
                <div class="space-y-6">
                    <div>
                        <h3 class="text-sm font-semibold text-gray-400 uppercase tracking-wider mb-2">Détails de la transaction</h3>
                        <div class="space-y-4">
                            <div>
                                <p class="text-sm text-gray-500">Date de vente</p>
                                <p class="text-lg text-gray-900">{{ vente.saleDate ? vente.saleDate|date('d/m/Y') : '-' }}</p>
                            </div>
                            <div>
                                <p class="text-sm text-gray-500">Acheteur</p>
                                <p class="text-lg text-gray-900">{{ vente.buyerName ?: 'Non spécifié' }}</p>
                            </div>
                            <div>
                                <p class="text-sm text-gray-500">Statut</p>
                                {% if vente.status %}
                                    <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium
                                        {% if vente.status == 'terminée' %}bg-green-100 text-green-800
                                        {% elseif vente.status == 'en attente' %}bg-yellow-100 text-yellow-800
                                        {% else %}bg-gray-100 text-gray-800{% endif %}">
                                        {{ vente.status }}
                                    </span>
                                {% else %}
                                    <span class="text-gray-400">Non défini</span>
                                {% endif %}
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            {# Récolte associée #}
            {% if vente.recolte %}
            <div class="mt-8 pt-8 border-t border-gray-100">
                <h3 class="text-sm font-semibold text-gray-400 uppercase tracking-wider mb-4">Récolte associée</h3>
                <div class="bg-lime-50 border border-lime-200 rounded-xl p-6">
                    <div class="flex items-center justify-between">
                        <div>
                            <p class="font-semibold text-gray-900">{{ vente.recolte.culture ? vente.recolte.culture.nomCulture : 'Récolte #' ~ vente.recolte.id }}</p>
                            <p class="text-sm text-gray-600 mt-1">
                                Quantité: {{ vente.recolte.quantity ? vente.recolte.quantity|number_format(2, ',', ' ') : '-' }} kg
                            </p>
                        </div>
                        <div class="text-right">
                            <p class="text-sm text-gray-500">Coût de production</p>
                            <p class="text-lg font-semibold text-lime-600">
                                {{ vente.recolte.coutProduction ? vente.recolte.coutProduction|number_format(2, ',', ' ') : '-' }} €
                            </p>
                        </div>
                    </div>
                </div>
            </div>
            {% endif %}
        </div>
    </div>
</div>
{% endblock %}
