{% extends 'back/base.html.twig' %}

{% block title %}Détails Mouvement{% endblock %}

{% block content %}
<div class="d-flex justify-content-between align-items-center mb-4">
    <h1 class="h3 m-0 text-dark">Mouvement #{{ mouvement_stock.idMouvement }}</h1>
    <div class="d-flex gap-2">
        <a href="{{ path('admin_mouvement_stock_edit', {'id_mouvement': mouvement_stock.idMouvement}) }}" class="btn btn-warning">
            <i class="bi bi-pencil"></i> Modifier
        </a>
        <a href="{{ path('admin_mouvement_stock_index') }}" class="btn btn-outline-secondary">
            <i class="bi bi-arrow-left"></i> Retour
        </a>
    </div>
</div>

<div class="card border-0 shadow-sm glass-panel">
    <div class="card-header bg-white border-0 py-3">
        <h2 class="h5 mb-0">Résumé du mouvement</h2>
    </div>
    <div class="card-body">
        <table class="table table-borderless mb-0">
            <tbody>
                <tr>
                    <th class="w-25">ID</th>
                    <td>{{ mouvement_stock.idMouvement }}</td>
                </tr>
                <tr>
                    <th>Type de Mouvement</th>
                    <td>
                        <span class="badge {% if mouvement_stock.typeMouvement == 'Entrée' %}bg-success{% else %}bg-warning text-dark{% endif %}">
                            {{ mouvement_stock.typeMouvement }}
                        </span>
                    </td>
                </tr>
                <tr>
                    <th>Produit</th>
                    <td>
                        <div class="d-flex flex-column">
                            <span class="fw-bold">{{ mouvement_stock.produit.nomProduit ?? 'Produit inconnu' }}</span>
                            <small class="text-muted">#{{ mouvement_stock.idProduit }}</small>
                        </div>
                    </td>
                </tr>
                <tr>
                    <th>Quantité</th>
                    <td class="fw-bold text-primary">{{ mouvement_stock.quantite }}</td>
                </tr>
                <tr>
                    <th>Date</th>
                    <td>{{ mouvement_stock.dateMouvement ? mouvement_stock.dateMouvement|date('d/m/Y H:i') : '-' }}</td>
                </tr>
                <tr>
                    <th>Motif</th>
                    <td>{{ mouvement_stock.motif }}</td>
                </tr>
                <tr>
                    <th>ID Utilisateur (Admin/Resp.)</th>
                    <td>{{ mouvement_stock.idUser }}</td>
                </tr>
            </tbody>
        </table>
    </div>
</div>
{% endblock %}
